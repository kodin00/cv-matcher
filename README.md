# CV Matcher

```bash
make install
make docker
```

`make install` copies `be/backend/.env.example` to `be/backend/.env` if it does not already exist.

`make docker` runs the frontend and backend with Docker Compose.
